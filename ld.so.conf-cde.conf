# CDE shared libraries are installed in the standard %{_libdir} (/usr/lib64);
# this file is a no-op placeholder reserved for future use (e.g. if any CDE
# library is moved out of the default search path).
